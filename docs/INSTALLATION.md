# Installation

See the main README for the quick path. This project is YAML-based and does not install through HACS.

The standalone dashboard calls the Home Assistant REST API. Create a Long-Lived Access Token from your Home Assistant profile and store it only on a trusted browser. Never commit a token.
