# Include with: input_number: !include input_numbers.yaml
# Duplicate and rename this four-helper group for every heating zone.

example_zone_temp_morning:
  name: Example Zone Temp Morning
  min: 5
  max: 27
  step: 0.5
  unit_of_measurement: "°C"
  mode: slider

example_zone_temp_midday:
  name: Example Zone Temp Midday
  min: 5
  max: 27
  step: 0.5
  unit_of_measurement: "°C"
  mode: slider

example_zone_temp_evening:
  name: Example Zone Temp Evening
  min: 5
  max: 27
  step: 0.5
  unit_of_measurement: "°C"
  mode: slider

example_zone_temp_night:
  name: Example Zone Temp Night
  min: 5
  max: 27
  step: 0.5
  unit_of_measurement: "°C"
  mode: slider
